import 'package:flutter/material.dart';

class CurrencySelectWidget extends StatelessWidget {
  const CurrencySelectWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return GestureDetector();
  }
}
